library(testthat)
library(dplyr)
library(stringr)
library(readr)
library(here)

# Run all test files inside tests/testthat/

# Run all test files inside tests/testthat/
test_dir(here("tests", "testthat"))
